# Data Architecture & Application Lifecycle

## Storage Layers

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Browser Tab (Runtime)                            │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                     WorkspaceState (in-memory)                       │  │
│  │                                                                      │  │
│  │  Global UI state          Per-database (active tab)    Cache          │  │
│  │  ├─ tabs[]                ├─ treeNodes[] (nested)      _snapshotCache │  │
│  │  ├─ activeTabId           ├─ edges[]                   (inactive tabs │  │
│  │  ├─ personas[]            ├─ constraints[]              held here to  │  │
│  │  ├─ workflows[]           ├─ constraintApplications[]   avoid reload) │  │
│  │  └─ connectedGraphs[]     ├─ focusId                                 │  │
│  │                           ├─ canvasNodePositions                      │  │
│  │                           ├─ canvasExpandedNodes                      │  │
│  │                           ├─ canvasSelected                           │  │
│  │                           ├─ canvasAlgorithm                          │  │
│  │                           └─ entityDrafts                             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│         │                          │                                       │
│         │ sync (debounced)         │ sync (debounced)                      │
│         ▼                          ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                  Local SurrealDB (mem:// WASM)                      │   │
│  │                  namespace: marlinspike                             │   │
│  │                                                                     │   │
│  │  ┌─────────────────┐    ┌────────────────────────────────────────┐  │   │
│  │  │   _ui database   │    │     Graph database (one per tab UUID)  │  │   │
│  │  │                  │    │                                        │  │   │
│  │  │  workspace:main  │    │  tree_node  (flat, parent links)      │  │   │
│  │  │  ├─ tabs         │    │  edge       (fromId → toId)           │  │   │
│  │  │  ├─ activeTabId  │    │  constraint (definitions)             │  │   │
│  │  │  ├─ personas     │    │  constraint_application (junctions)   │  │   │
│  │  │  └─ workflows    │    │  canvas_state:main (positions, focus) │  │   │
│  │  │                  │    │                                        │  │   │
│  │  │  db_registry     │    └────────────────────────────────────────┘  │   │
│  │  │  ├─ uuid         │         ▲                                     │   │
│  │  │  ├─ name         │         │ flattenTree() / buildTree()         │   │
│  │  │  └─ lastOpened   │         │ (flat rows ↔ nested TreeNode[])     │   │
│  │  └─────────────────┘─────────────────────────────────────────────┘  │   │
│  │         │                          │                                │   │
│  └─────────┼──────────────────────────┼────────────────────────────────┘   │
│            │ exportDb()               │ exportDb()                         │
│            ▼                          ▼                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                IndexedDB  (marlinspike_snapshots)                    │   │
│  │                                                                     │   │
│  │  "ui"          → SurrealQL dump of _ui database                     │   │
│  │  "db:{uuid}"   → SurrealQL dump of each graph database              │   │
│  │                                                                     │   │
│  │  (Workaround for SurrealDB WASM indxdb:// engine bug #5712)         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Remote SurrealDB Pool  (WebSocket connections)                     │   │
│  │                                                                     │   │
│  │  remoteConnections: Map<id, Surreal>                                │   │
│  │  ├─ Config read from workspace root node: data.connection           │   │
│  │  ├─ Bootstrapped reactively when config changes                     │   │
│  │  └─ Accessed via getDb(connectionId)                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─────────────────┐                                                      │
│  │  localStorage    │  Legacy — migrated to SurrealDB on first run         │
│  │  (deprecated)    │  Key: marlinspike.workspace                          │
│  └─────────────────┘                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Application Lifecycle

```
   App Mount                    User Interaction              Page Unload
   ─────────                    ────────────────              ───────────
       │                              │                            │
       ▼                              │                            │
  ┌─────────┐                         │                            │
  │  INIT   │                         │                            │
  │         │                         │                            │
  │ 1. initSurreal()                  │                            │
  │    └─ connect mem://              │                            │
  │                                   │                            │
  │ 2. Restore _ui from IndexedDB    │                            │
  │    └─ importDb(loadDump("ui"))    │                            │
  │                                   │                            │
  │ 3. Check db_registry             │                            │
  │    └─ migrate localStorage       │                            │
  │       if first run               │                            │
  │                                   │                            │
  │ 4. Restore active graph DB       │                            │
  │    └─ importDb(loadDump(uuid))   │                            │
  │                                   │                            │
  │ 5. Load graph data               │                            │
  │    ├─ loadAllNodes() → buildTree  │                            │
  │    ├─ loadAllEdges()             │                            │
  │    ├─ loadAllConstraints()       │                            │
  │    ├─ loadAllApplications()      │                            │
  │    └─ loadCanvasState()          │                            │
  │                                   │                            │
  │ 6. ensureWorkspaceRoot()         │                            │
  │    ensureWorkspaceConstraint()   │                            │
  │                                   │                            │
  │ 7. setWs(state)                  │                            │
  │    setSyncBaseline(state)        │                            │
  └─────────┘                         │                            │
       │                              │                            │
       ▼                              ▼                            │
  ┌──────────────────────────────────────────┐                     │
  │              REACTIVE LOOP               │                     │
  │                                          │                     │
  │  useEffect([ws]) on every state change:  │                     │
  │                                          │                     │
  │  ┌─────────────────────────────────┐     │                     │
  │  │     scheduleSyncToDb(ws)        │     │                     │
  │  │                                 │     │                     │
  │  │  Debounce: 500ms trailing,      │     │                     │
  │  │            2000ms max wait      │     │                     │
  │  │                                 │     │                     │
  │  │  Diff prev vs next:             │     │                     │
  │  │  ├─ Graph changed?             │     │                     │
  │  │  │  └─ useDatabase(uuid)       │     │                     │
  │  │  │    flattenTree → diff →     │     │                     │
  │  │  │    UPSERT/DELETE nodes,     │     │                     │
  │  │  │    edges, constraints       │     │                     │
  │  │  │    exportDb → saveDump      │     │                     │
  │  │  │                              │     │                     │
  │  │  ├─ Canvas changed?            │     │                     │
  │  │  │  └─ saveCanvasState()       │     │                     │
  │  │  │                              │     │                     │
  │  │  └─ UI changed?               │     │                     │
  │  │     └─ useUiDb()              │     │                     │
  │  │       saveWorkspaceUi()        │     │                     │
  │  │       exportDb → saveDump      │     │                     │
  │  └─────────────────────────────────┘     │                     │
  │                                          │                     │
  │  ┌─────────────────────────────────┐     │                     │
  │  │  Remote connection bootstrap    │     │                     │
  │  │                                 │     │                     │
  │  │  getConnectionConfig(ws)        │     │                     │
  │  │  ├─ reads root.data.connection  │     │                     │
  │  │  ├─ JSON snapshot diff          │     │                     │
  │  │  └─ connectRemote() or         │     │                     │
  │  │     disconnectRemote()          │     │                     │
  │  └─────────────────────────────────┘     │                     │
  │                                          │                     │
  └──────────────────────────────────────────┘                     │
       │                                                           │
       │  Tab switch                                               │
       ▼                                                           ▼
  ┌─────────────┐                                         ┌──────────────┐
  │  TAB SWITCH  │                                         │  UNLOAD      │
  │              │                                         │              │
  │ 1. flushSync(current)                                  │ flushSync()  │
  │ 2. Snapshot current tab                                │ (best-effort │
  │    → _snapshotCache                                    │  async)      │
  │ 3. Load target tab:                                    └──────────────┘
  │    ├─ from cache (fast) or
  │    └─ loadDatabaseSnapshot()
  │       (restore from IndexedDB)
  │ 4. Reset sync baseline
  └─────────────┘
```

## Code View Data Flow

```
  ┌───────────────────────────────────────────────────────────────────────┐
  │                        Code Panel                                    │
  │                                                                      │
  │   WorkspaceState                                                     │
  │        │                                                             │
  │        │ emitWorkspace(ws)                                           │
  │        │ ├─ focusId=null  → (def RootLabel [...all children...])     │
  │        │ └─ focusId=root  → children only (no wrapper)               │
  │        ▼                                                             │
  │   ┌──────────┐        graphToSpike(nodes, edges)                     │
  │   │  Clojure  │        ├─ def  → structural container (no edges)     │
  │   │  source   │        ├─ defn → dataflow container (let bindings)   │
  │   │  text     │        └─ ^{:id :uri :data} reader metadata          │
  │   └──────────┘                                                       │
  │        │                                                             │
  │        │ Cmd+Enter (apply)                                           │
  │        │                                                             │
  │        │ parseWorkspace(code, ws)                                    │
  │        │ ├─ spikeToGraph(code) → nodes, edges, errors                │
  │        │ ├─ Match parsed nodes to existing:                          │
  │        │ │  ├─ By ^{:id "uuid"} metadata (global tree search)        │
  │        │ │  ├─ By label match (within siblings)                      │
  │        │ │  └─ Fresh UUID if no match                                │
  │        │ ├─ Remap edge IDs through merge map                         │
  │        │ └─ Re-wrap in workspace root (preserves rootNodeId)         │
  │        ▼                                                             │
  │   setWs(mergedState) → triggers reactive sync loop                   │
  └───────────────────────────────────────────────────────────────────────┘
```

## Data Ownership by Database

```
  getDb()                                    getDb(connectionId)
     │                                              │
     ▼                                              ▼
  Local SurrealDB (mem://)                   Remote SurrealDB (ws://)
  ┌────────────────────────────────┐         ┌──────────────────────┐
  │                                │         │                      │
  │  _ui database                  │         │  External database   │
  │  ├─ tabs, activeTabId         │         │  ├─ tree_node        │
  │  ├─ personas, workflows       │         │  ├─ edge             │
  │  ├─ connectedGraphs           │         │  ├─ constraint       │
  │  └─ db_registry               │         │  └─ ...              │
  │                                │         │                      │
  │  Graph databases (per-tab)     │         │  (read/write via     │
  │  ├─ tree_node (flat rows)     │         │   unified getDb(id)  │
  │  ├─ edge                      │         │   interface)         │
  │  ├─ constraint                │         │                      │
  │  ├─ constraint_application    │         └──────────────────────┘
  │  └─ canvas_state              │
  │                                │
  └────────────────────────────────┘
         │
         │ exportDb() / importDb()
         ▼
  IndexedDB (persistence bridge)
  ┌────────────────────────────────┐
  │  "ui"       → SurrealQL dump   │
  │  "db:{uuid}" → SurrealQL dump  │
  └────────────────────────────────┘
```
